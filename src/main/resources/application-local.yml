server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
 #   url: jdbc:mysql://localhost:3306/test?serverTimezone=Asia/Seoul
#    username: root
#    password:


  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
